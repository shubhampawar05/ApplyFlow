// Purpose: own deterministic application lifecycle business logic.
// Codex task: implement creation, readiness validation, sending authorization, and status transitions.
// Never let AI code directly mutate application state or send email.
// Enforce authenticated user ownership for every operation.
// Add unit tests for every state transition and failure path.
