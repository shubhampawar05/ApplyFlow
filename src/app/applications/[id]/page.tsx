import { AppShell } from "@/components/app-shell";

export default function ApplicationDetailPage() {
  return <AppShell activePath=""><p className="eyebrow">Application</p><h1>Application details will appear here.</h1><p className="lede">This screen will become available when the reviewed application workflow is connected to persistence.</p></AppShell>;
}
