import { AppShell } from "@/components/app-shell";

export default function SettingsPage() {
  return <AppShell activePath="/settings"><p className="eyebrow">Settings</p><h1>Set up your application profile.</h1><p className="lede">Resume and Gmail connection controls will be added in the next setup milestone.</p></AppShell>;
}
