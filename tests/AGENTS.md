# Testing instructions

Tests must describe observable behavior rather than implementation details.
Use deterministic fixtures for AI responses and provider adapters.
Critical send-flow tests must verify explicit human approval.
Include authorization and failure-path coverage for new endpoints.
Run the documented test suite before completing a feature.
