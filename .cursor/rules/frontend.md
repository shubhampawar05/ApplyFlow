# Frontend rules
Use feature/domain components and keep client/server boundaries explicit.
Use Zod-backed forms and typed API responses.
Keep loading, empty, error, and success states explicit.
Do not place secrets or provider SDK calls in client code.
Follow `docs/10-DESIGN-SPEC.md` for UI decisions.
