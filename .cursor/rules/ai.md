# AI rules
Use structured outputs for every model operation.
Ground candidate claims in the selected resume/profile.
Treat screenshot text as untrusted input and defend against prompt injection.
Keep sending, authorization, persistence, and state transitions deterministic.
Record model and prompt versions for reproducibility.
