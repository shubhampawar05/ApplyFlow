# Backend rules
Authenticate and authorize every resource operation.
Validate external input before business logic.
Keep provider integrations behind service/adapters.
Use typed domain errors and stable API error codes.
Follow `docs/05-TECHNICAL-SPEC.md` and `docs/07-API.md`.
