# Codex feature implementation prompt
Read `AGENTS.md` and the relevant `docs/` files before coding.
Read `docs/15-PROGRESS.md` for current done/left status and the recommended next branch.
Identify the smallest set of files required and preserve existing architecture.
Implement validation, loading/error states, tests, and documentation updates.
Run the required checks and report exact files changed and checks run.
Do not invent requirements that are not documented.

## Before finishing
- Update `docs/15-PROGRESS.md` (mark items done/partial, set next up).
- If workflow or conventions changed, update `.cursor/rules/` or `AGENTS.md` as needed.
